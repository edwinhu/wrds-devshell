[project]
name = "wrds-data-science"
description = "Data science packages for WRDS portable environment"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
# Core data science environment
euporie = ">=2.8"
sas_kernel = "*"

# Shell
zsh = "*"

[tasks]

